<div>
  <h1>¡Hola, {{user.name}} {{user.lastName}}!¡Como andas?!</h1>
  {{#if isAdmin}}
    {{#each food}}
      <div>
        <p>{{this.name}} : {{this.price}}</p>
      </div>
    {{/each}}
  {{else}}
    <p>No tenés permisos de Administrador</p>
  {{/if}}
</div>
<script src="/js/index.js"></script>